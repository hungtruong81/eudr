export enum STATUS_APPROVE {
  STATUS_PENDING_APPROVE = 0,
  STATUS_APPROVE = 1,
  STATUS_REJECT = 2,
}

export enum OTP_TYPE {
  REIGSTER = "register",
}

export enum DISPLAY_TYPE {
  FARMER = "farmer",
  PURCHASER = "purchaser",
  TRADER = "trader",
  INSPECTOR = "inspector",
  COMPANY = "company",
  ADMIN = "admin",
  WORKER = "worker",
}
